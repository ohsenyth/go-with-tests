package integers

// Add takes two integers and returns the sum of them
func Add(x, y int) int {
	// return 0; // step 1: make the test fail
	// return 4; // step 2: write minimal amount of code to make the test pass
	return x + y // step 3: write the code properly
}
